
    <!-- header.hbs -->
<header>
    <div class="brand-container">
        <div class="logo-container">
            <a href="{{@site.url}}" title="{{@site.title}} &mdash; {{@site.description}}" target="_top"><figure class="logo">
                <img src="{{@site.logo}}" alt="{{@site.title}}" />
            </figure>
            </a>
        </div>
        <nav class="desktop-nav">
            {{navigation}}
        </nav>
        
    </div>
</header>