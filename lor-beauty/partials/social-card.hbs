{{!--
{{#social_accounts @site}}
{{#social_accounts author}}
--}}
<a href="{{href}}" target="_blank" rel="noopener" aria-label="{{t (name)}}">
    {{#> (concat "icons/" type)}}
    {{!-- Fallback rendered when no per-platform icon partial exists --}}
    <span class="icon icon-web">{{t (name)}}</span>
    {{/undefined}}
</a>{{else}}<p class="no-social-accounts">{{t "No social accounts connected yet."}}</p>
{{!--
{{/social_accounts}}
--}}