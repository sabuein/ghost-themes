<!-- partials/recommendations.hbs -->
{{#if recommendations}}<ul>
    {{#foreach recommendations as |rec|}}<li class="recommendation">
        <a href="{{rec.url}}" data-recommendation="{{rec.id}}" target="_blank" rel="noopener">
            <figure class="recommendation-favicon">
                <img src="{{#if rec.favicon}}{{rec.favicon}}{{else}}https://placehold.co/80x40{{/if}}" alt="{{rec.title}}" loading="lazy" onerror="this.style.display='none';" />
            </figure>
            <h5 class="recommendation-title">{{rec.title}}</h5>
            <span class="recommendation-url">{{readable_url rec.url}}</span>
            <p class="recommendation-description">{{rec.description}}</p>
        </a>
    </li>{{/foreach}}
</ul>
<button data-portal="recommendations">Show all recommendations</button><!-- href="/#/portal/recommendations" -->{{else}}<pre>This Ghost instance does not have any recommendations yet&period;</pre>{{/if}}